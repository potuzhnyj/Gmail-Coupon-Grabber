{
  "name": "gmail-coupon-grabber",
  "version": "1.0.0",
  "description": "Scan Gmail for promo emails, follow redirects, extract URL params, generate HTML report",
  "author": "Ivan Moshak",
  "license": "MIT",
  "scripts": {
    "start": "node --no-warnings grab.js",
    "debug": "node --no-warnings debug.js",
    "debug:10": "node --no-warnings debug.js 10"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "imapflow": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
